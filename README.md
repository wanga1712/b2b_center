# B2B AutoDesk — CRM система для управления коммерческими предложениями

Современная CRM система для автоматизации бизнес-процессов, визуализация и структура которой основаны на Salesforce. Система предназначена для управления коммерческими предложениями, клиентами, торгами, отгрузками и задачами.

## 📋 Описание проекта

B2B AutoDesk — это комплексная система для автоматизации бизнес-процессов в B2B сегменте, включающая:

- **Коммерческие предложения (КП)** — создание и управление коммерческими предложениями с автоматическим расчетом цен, скидок и упаковки
- **Торги** — участие в торгах и управление заявками
- **Отгрузка** — управление отгрузками и расчет стоимости доставки
- **Клиенты** — управление базой клиентов
- **Задачи** — управление задачами и проектами
- **AI Ассистент** — интеллектуальный помощник на базе OpenRouter AI

## 🏗️ Архитектура проекта

Проект построен на основе модульной архитектуры с четким разделением ответственности:

```
pythonProject89/
├── config/              # Конфигурация приложения
│   ├── database.py      # Настройки подключения к БД
│   └── settings.py      # Общие настройки приложения
├── core/                # Ядро приложения
│   ├── database.py      # Менеджер базы данных
│   ├── models.py        # Модели данных
│   └── exceptions.py    # Пользовательские исключения
├── modules/              # Модули функциональности
│   ├── kp/              # Коммерческие предложения
│   ├── bids/            # Торги
│   ├── shipping/        # Отгрузка
│   ├── clients/         # Клиенты
│   ├── tasks/           # Задачи
│   ├── ii/              # AI Ассистент
│   └── styles/          # Стили приложения
├── services/            # Бизнес-логика
│   ├── search_service.py      # Поиск товаров
│   ├── quotation_service.py   # Работа с коммерческими предложениями
│   └── delivery_calculator.py # Расчет доставки
└── ui/                  # Пользовательский интерфейс
    └── main_window.py   # Главное окно приложения
```

## 🗄️ База данных

Система использует PostgreSQL для хранения данных. Основные таблицы:

- **categories** — категории товаров
- **subcategories** — подкатегории товаров
- **manufacturers** — производители
- **products** — товары с техническими характеристиками
- **product_pricing** — цены на товары (различные варианты упаковки)
- **product_packaging** — информация об упаковке (количество на паллете)

## 🚀 Установка и запуск

### Требования

- Python 3.11+
- PostgreSQL 12+
- PyQt5 или PySide6
- rapidfuzz (для нечеткого поиска с обработкой опечаток)

### Установка зависимостей

```bash
# Активация виртуального окружения
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### Настройка окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# ============================================
# База данных (каталог продукции)
# ============================================
DB_HOST=localhost
DB_DATABASE=commercial_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_PORT=5432

# ============================================
# База данных (tender_monitor)
# ============================================
TENDER_MONITOR_DB_HOST=localhost
TENDER_MONITOR_DB_DATABASE=tender_monitor
TENDER_MONITOR_DB_USER=postgres
TENDER_MONITOR_DB_PASSWORD=your_password
TENDER_MONITOR_DB_PORT=5432

# ============================================
# Доставка
# ============================================
DELIVERY_COST_PER_KM=15.0
DELIVERY_MANIPULATOR_COST=5000.0
DELIVERY_REAR_LOADER_COST=3000.0
DELIVERY_SIDE_LOADER_COST=4000.0
DELIVERY_GAZELLE_CAPACITY=50

# ============================================
# Пользовательский интерфейс
# ============================================
UI_WINDOW_SIZE=1200x800
UI_BACKGROUND_COLOR=#F5F5F5
UI_ACCENT_COLOR=#2066B0
UI_FONT_FAMILY=Arial
UI_FONT_SIZE=14

# ============================================
# Приложение
# ============================================
APP_NAME=B2B AutoDesk
APP_VERSION=1.0.0
LOG_LEVEL=INFO
LOG_ROTATION=10 MB
LOG_RETENTION=30 days

# ============================================
# Локальные пути и инструменты
# ============================================
# Путь к директории для скачивания документов из ЕИС
# Пример: DOCUMENT_DOWNLOAD_DIR=C:\Users\username\YandexDisk\Файлы_из_ЕИС\Документация
DOCUMENT_DOWNLOAD_DIR=

# Путь к директории WinRAR (для распаковки архивов)
# Пример: WINRAR_PATH=C:\Program Files\WinRAR
WINRAR_PATH=C:\Program Files\WinRAR

# Путь к инструменту UnRAR (опционально, если отличается от стандартного)
# Пример: UNRAR_TOOL=C:\Program Files\WinRAR\UnRAR.exe
UNRAR_TOOL=
```

**Важно:** Все локальные пути должны быть настроены через переменные окружения в `.env` файле. Хардкод путей в коде не допускается.

### Запуск приложения

```bash
python main.py
```

## 📦 Модуль коммерческих предложений (КП)

### Функциональность

Модуль коммерческих предложений позволяет:

1. **Умный поиск товаров** — поиск по категориям, подкатегориям, производителям и названию с поддержкой нечеткого поиска (fuzzy search) для обработки опечаток
2. **Добавление товаров** в коммерческое предложение
3. **Расчет количества упаковки** — при вводе веса (кг) система автоматически рассчитывает количество мешков с округлением вверх
4. **Применение скидок** — индивидуальные и глобальные скидки на позиции
5. **Расчет итоговой суммы** — автоматический пересчет при изменении количества или скидок
6. **Конвертация валют** — автоматическая конвертация цен из EUR в RUB для производителя Гидрозо (курс: 1 EUR = 100 RUB)

### Пример использования

1. Выберите категорию, подкатегорию и производителя в фильтрах (каскадный выбор)
2. Найдите нужный товар в таблице:
   - Используйте поиск по названию (поддерживает опечатки, например "ipone" найдет "iphone")
   - При загрузке приложения все товары отображаются автоматически
3. Выберите тип ввода: количество (шт) или вес (кг)
4. Введите количество или вес
5. Система автоматически рассчитает количество упаковки (мешков) при вводе веса
6. Добавьте товар в коммерческое предложение
7. При необходимости примените скидку (индивидуальную или глобальную)
8. Просмотрите итоговую сумму

### Нечеткий поиск (Fuzzy Search)

Система поддерживает умный поиск с обработкой опечаток:

- **Примеры работы:**
  - Ввод "ipone" → найдет "iPhone"
  - Ввод "самсунг" → найдет "Samsung"
  - Ввод "гидроизоляция" → найдет товары с "гидроизоляция" даже при опечатках

- **Технология:** Используется библиотека `rapidfuzz` для быстрого и точного нечеткого поиска
- **Порог совпадения:** 70% (настраивается)
- **Приоритет:** Сначала показываются точные совпадения, затем нечеткие по релевантности

### Расчет упаковки

При вводе веса товара (например, 255 кг) система:
- Определяет вес одной единицы упаковки (например, 20 кг)
- Рассчитывает количество мешков: `ceil(255 / 20) = 13 мешков`
- Отображает рассчитанное количество в интерфейсе

## 🎨 Стилизация

Приложение использует модульную систему стилей в `modules/styles/`:
- Единые стили для всех компонентов
- Адаптивное масштабирование под разные разрешения экрана
- Цветовая палитра в стиле Salesforce

## 🔧 Разработка

### Структура кода

- **Модели данных** (`core/models.py`) — dataclass модели для типизации
- **Репозитории** (`core/database.py`) — работа с базой данных
- **Сервисы** (`services/`) — бизнес-логика
- **Виджеты** (`modules/*/widget.py`) — пользовательский интерфейс
- **Логика** (`modules/*/logic.py`) — обработка событий

### Соглашения о коде

- Использование camelCase для переменных и функций
- PascalCase для классов
- Комментарии к функциям и классам
- Обработка ошибок через пользовательские исключения
- Логирование через loguru

### Конфигурация и пути

**Важно:** Все локальные пути должны быть настроены через переменные окружения в `.env` файле. Хардкод путей в коде не допускается.

- Все пути к директориям и файлам настраиваются через `.env`
- В коде пути указываются только в комментариях для справки
- Используйте `config.settings.config` для доступа к настройкам

### Работа с архивами

Система поддерживает распаковку архивов следующих форматов:
- **ZIP** — с правильной обработкой кодировок (cp437 → cp866)
- **RAR** — с поддержкой многофайловых архивов
- **7Z** — полная поддержка формата 7z

Для работы с RAR архивами требуется установленный WinRAR или UnRAR. Путь настраивается через переменные окружения:
- `WINRAR_PATH` — путь к директории WinRAR
- `UNRAR_TOOL` — путь к конкретному инструменту (опционально)

## 📝 Лицензия

Проект разработан для внутреннего использования.

## 👥 Авторы

Разработано командой B2B AutoDesk.
