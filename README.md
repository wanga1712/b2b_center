# B2B AutoDesk — CRM система для управления коммерческими предложениями

Современная CRM система для автоматизации бизнес-процессов, визуализация и структура которой основаны на Salesforce. Система предназначена для управления коммерческими предложениями, клиентами, торгами, отгрузками и задачами.

## 📋 Описание проекта

B2B AutoDesk — это комплексная система для автоматизации бизнес-процессов в B2B сегменте, включающая:

- **Коммерческие предложения (КП)** — создание и управление коммерческими предложениями с автоматическим расчетом цен, скидок и упаковки
- **Торги** — участие в торгах и управление заявками
- **Отгрузка** — управление отгрузками и расчет стоимости доставки
- **Клиенты** — управление базой клиентов
- **Задачи** — управление задачами и проектами
- **AI Ассистент** — интеллектуальный помощник на базе OpenRouter AI
- **Воронка продаж** — управление сделками через канбан-доски (участие в торгах, поставка материалов, субподрядные работы)

## 🏗️ Архитектура проекта

Проект построен на основе модульной архитектуры с четким разделением ответственности:

```
pythonProject89/
├── config/              # Конфигурация приложения
│   ├── database.py      # Настройки подключения к БД
│   └── settings.py      # Общие настройки приложения
├── core/                # Ядро приложения
│   ├── database.py      # Менеджер базы данных
│   ├── models.py        # Модели данных
│   └── exceptions.py    # Пользовательские исключения
├── modules/              # Модули функциональности
│   ├── kp/              # Коммерческие предложения
│   ├── bids/            # Торги
│   ├── shipping/        # Отгрузка
│   ├── clients/         # Клиенты
│   ├── tasks/           # Задачи
│   ├── ii/              # AI Ассистент
│   ├── crm/             # CRM модули
│   │   └── sales_funnel/ # Воронки продаж
│   │       ├── models.py # Модели данных
│   │       ├── pipeline_repository.py # Репозиторий этапов
│   │       ├── deal_repository.py # Репозиторий сделок
│   │       ├── submenu_widget.py # Подменю воронок
│   │       ├── funnel_widget.py # Виджет воронки
│   │       ├── kanban_board.py # Канбан-доска
│   │       ├── kanban_column.py # Колонка канбана
│   │       └── deal_card.py # Карточка сделки
│   └── styles/          # Стили приложения
├── services/            # Бизнес-логика
│   ├── search_service.py      # Поиск товаров
│   ├── quotation_service.py   # Работа с коммерческими предложениями
│   ├── delivery_calculator.py # Расчет доставки
│   ├── document_search_service.py # Поиск товаров в документации торгов
│   └── document_search/       # Модули поиска по документации
│       ├── document_downloader.py  # Загрузка документов
│       ├── document_selector.py    # Выбор документов для обработки
│       ├── archive_extractor.py   # Извлечение архивов
│       ├── excel_parser.py        # Парсинг Excel файлов
│       └── match_finder.py        # Поиск совпадений товаров
└── ui/                  # Пользовательский интерфейс
    └── main_window.py   # Главное окно приложения
```

## 🧱 Принципы модульного проектирования

### Основные правила

- **Независимость модулей** — изменение одного модуля не должно тянуть за собой правки в других. Стремимся к слабым связям и явным контрактам.
- **Четко определённый интерфейс** — каждый модуль предоставляет понятный публичный API: какие методы доступны, какие данные возвращаются.
- **Высокоуровневые задачи** — модуль решает одну бизнес-задачу и не содержит избыточной логики (Single Responsibility).
- **Избегаем жёстких зависимостей** — используем абстракции, внедрение зависимостей и фабрики, чтобы один модуль как можно меньше знал о внутренностях другого.
- **Повторное использование кода** — выносим общие компоненты в пакеты/библиотеки, чтобы переиспользовать их в других частях проекта или в новых проектах.

### Этапы проектирования

1. **Нисходящее проектирование** — сперва описываем работу головного модуля (оркестратор), остальные модули заменяем заглушками. Затем поэтапно реализуем и подключаем реальные реализации.
2. **Восходящее проектирование** — начинаем с модулей нижнего уровня (парсеры, DAO), тестируем их изолированно и только после этого собираем из них высокоуровневую логику.

На практике комбинируем оба подхода: критичные низкоуровневые модули разрабатываем снизу вверх, а пользовательские сценарии — сверху вниз.

### Инструменты и подходы

- **Пакеты/модули языка** — в Python используем пакеты с `__init__.py`, чтобы группировать области ответственности.
- **Абстракции и интерфейсы** — базовые классы, протоколы typing, dataclasses для контракта между слоями.
- **Внедрение зависимостей (DI)** — передаём зависимости через конструкторы/фабрики, избегаем жёсткого импорта внутри классов.
- **Стандартные библиотеки и утилиты** — по максимуму используем готовые модули, чтобы не дублировать код.

Новые фичи и рефакторинг должны соответствовать этим принципам. Перед интеграцией крупных изменений сравниваем архитектурные решения с перечнем выше.

## 🗄️ База данных

Система использует PostgreSQL для хранения данных. Основные таблицы:

- **categories** — категории товаров
- **subcategories** — подкатегории товаров
- **manufacturers** — производители
- **products** — товары с техническими характеристиками
- **product_pricing** — цены на товары (различные варианты упаковки)
- **product_packaging** — информация об упаковке (количество на паллете)

### Таблицы мониторинга документов торгов

- **tender_document_matches** — агрегированные результаты поиска совпадений по каждому торгу. Хранит идентификатор торга, реестр (44-ФЗ/223-ФЗ), количество совпадений, процент совпадения, время обработки, общий размер обработанных файлов. По этой таблице карточки торгов отображают актуальный статус.
- **tender_document_match_details** — подробные строки совпадений: название продукта, лист/строка/ячейка Excel, рассчитанный score (100%, 85% и т.д.). Используется для аналитики и подсветки конкретных товаров.

Скрипт `scripts/ensure_tender_tables.py` создает таблицы и индексы, если их еще нет. Для полного сброса данных предусмотрен `scripts/clear_tender_match_data.py`.

## 🚀 Установка и запуск

### Требования

- Python 3.11+
- PostgreSQL 12+
- PyQt5 или PySide6
- rapidfuzz (для нечеткого поиска с обработкой опечаток)

### Установка зависимостей

```bash
# Активация виртуального окружения
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### Настройка окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# ============================================
# База данных (каталог продукции)
# ============================================
DB_HOST=localhost
DB_DATABASE=commercial_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_PORT=5432

# ============================================
# База данных (tender_monitor)
# ============================================
TENDER_MONITOR_DB_HOST=localhost
TENDER_MONITOR_DB_DATABASE=tender_monitor
TENDER_MONITOR_DB_USER=postgres
TENDER_MONITOR_DB_PASSWORD=your_password
TENDER_MONITOR_DB_PORT=5432

# ============================================
# Доставка
# ============================================
DELIVERY_COST_PER_KM=15.0
DELIVERY_MANIPULATOR_COST=5000.0
DELIVERY_REAR_LOADER_COST=3000.0
DELIVERY_SIDE_LOADER_COST=4000.0
DELIVERY_GAZELLE_CAPACITY=50

# ============================================
# Пользовательский интерфейс
# ============================================
UI_WINDOW_SIZE=1200x800
UI_BACKGROUND_COLOR=#F5F5F5
UI_ACCENT_COLOR=#2066B0
UI_FONT_FAMILY=Arial
UI_FONT_SIZE=14

# ============================================
# Приложение
# ============================================
APP_NAME=B2B AutoDesk
APP_VERSION=1.0.0
LOG_LEVEL=INFO
LOG_ROTATION=10 MB
LOG_RETENTION=30 days

# ============================================
# Локальные пути и инструменты
# ============================================
# Путь к директории для скачивания документов из ЕИС
# Пример: DOCUMENT_DOWNLOAD_DIR=C:\Projects\Documents\Tenders
DOCUMENT_DOWNLOAD_DIR=

# Путь к директории WinRAR (для распаковки архивов)
# Пример: WINRAR_PATH=C:\Program Files\WinRAR
WINRAR_PATH=

# Путь к инструменту UnRAR
# Пример: UNRAR_TOOL=C:\Program Files\WinRAR\UnRAR.exe
# Если не указан, используется WINRAR_PATH\UnRAR.exe
UNRAR_TOOL=
```

**Важно:** Все локальные пути должны быть настроены через переменные окружения в `.env` файле. Хардкод путей в коде не допускается.

### Запуск приложения

```bash
python main.py
```

## 📦 Модуль коммерческих предложений (КП)

### Функциональность

Модуль коммерческих предложений позволяет:

1. **Умный поиск товаров** — поиск по категориям, подкатегориям, производителям и названию с поддержкой нечеткого поиска (fuzzy search) для обработки опечаток
2. **Добавление товаров** в коммерческое предложение
3. **Расчет количества упаковки** — при вводе веса (кг) система автоматически рассчитывает количество мешков с округлением вверх
4. **Применение скидок** — индивидуальные и глобальные скидки на позиции
5. **Расчет итоговой суммы** — автоматический пересчет при изменении количества или скидок
6. **Конвертация валют** — автоматическая конвертация цен из EUR в RUB для производителя Гидрозо (курс: 1 EUR = 100 RUB)

### Пример использования

1. Выберите категорию, подкатегорию и производителя в фильтрах (каскадный выбор)
2. Найдите нужный товар в таблице:
   - Используйте поиск по названию (поддерживает опечатки, например "ipone" найдет "iphone")
   - При загрузке приложения все товары отображаются автоматически
3. Выберите тип ввода: количество (шт) или вес (кг)
4. Введите количество или вес
5. Система автоматически рассчитает количество упаковки (мешков) при вводе веса
6. Добавьте товар в коммерческое предложение
7. При необходимости примените скидку (индивидуальную или глобальную)
8. Просмотрите итоговую сумму

### Нечеткий поиск (Fuzzy Search)

Система поддерживает умный поиск с обработкой опечаток:

- **Примеры работы:**
  - Ввод "ipone" → найдет "iPhone"
  - Ввод "самсунг" → найдет "Samsung"
  - Ввод "гидроизоляция" → найдет товары с "гидроизоляция" даже при опечатках

- **Технология:** Используется библиотека `rapidfuzz` для быстрого и точного нечеткого поиска
- **Порог совпадения:** 70% (настраивается)
- **Приоритет:** Сначала показываются точные совпадения, затем нечеткие по релевантности

### Расчет упаковки

При вводе веса товара (например, 255 кг) система:
- Определяет вес одной единицы упаковки (например, 20 кг)
- Рассчитывает количество мешков: `ceil(255 / 20) = 13 мешков`
- Отображает рассчитанное количество в интерфейсе

## 🎨 Стилизация

Приложение использует модульную систему стилей в `modules/styles/`:
- **general_styles.py** — единые стили для всех компонентов (кнопки, метки, фреймы, таблицы)
- **ui_config.py** — централизованная конфигурация окон и диалогов PyQt5
- **scaling.py** — адаптивное масштабирование под разные разрешения экрана
- Цветовая палитра в стиле Bitrix24/Salesforce

### Принципы стилизации

- Все настройки окон (размеры, заголовки) должны использовать функции из `ui_config.py`
- Все стили должны использовать функции из `general_styles.py`
- Запрещено дублирование стилей — если стиль повторяется более 2 раз, добавить функцию
- Принцип DRY (Don't Repeat Yourself) — одно действие = один метод

## 🔧 Разработка

### Структура кода

- **Модели данных** (`core/models.py`) — dataclass модели для типизации
- **Репозитории** (`core/database.py`) — работа с базой данных
- **Сервисы** (`services/`) — бизнес-логика
- **Виджеты** (`modules/*/widget.py`) — пользовательский интерфейс
- **Логика** (`modules/*/logic.py`) — обработка событий

### Соглашения о коде

- Использование camelCase для переменных и функций
- PascalCase для классов
- Комментарии к функциям и классам
- Обработка ошибок через пользовательские исключения
- Логирование через loguru

### Конфигурация и пути

**Важно:** Все локальные пути должны быть настроены через переменные окружения в `.env` файле. Хардкод путей в коде не допускается.

- Все пути к директориям и файлам настраиваются через `.env`
- В коде пути указываются только в комментариях для справки
- Используйте `config.settings.config` для доступа к настройкам

### Работа с архивами

Система поддерживает распаковку архивов следующих форматов:
- **ZIP** — с правильной обработкой кодировок (cp437 → cp866)
- **RAR** — с поддержкой многофайловых архивов
- **7Z** — полная поддержка формата 7z

Для работы с RAR архивами требуется установленный WinRAR или UnRAR. Путь настраивается через переменные окружения:
- `WINRAR_PATH` — путь к директории WinRAR
- `UNRAR_TOOL` — путь к конкретному инструменту (опционально)
- **Важно:** Убедитесь, что пути к WinRAR/UnRAR указаны в `.env` файле, иначе распаковка RAR архивов будет недоступна.

## 🔍 Модуль поиска по документации торгов

### Описание

Модуль автоматически анализирует документацию торгов (сметы, Excel файлы) и находит совпадения с товарами из базы данных. Поддерживает работу с архивами (ZIP, RAR, 7Z) и Excel файлами (.xlsx, .xls).

### Функциональность

1. **Автоматический выбор документов** — система выбирает:
   - Документы со словом "смета" в названии
   - Все Excel файлы (.xlsx, .xls)

2. **Параллельная загрузка** — документы скачиваются параллельно с прогресс-баром

3. **Распаковка архивов** — автоматическая распаковка ZIP, RAR, 7Z архивов (включая многочастные)

4. **Умный поиск товаров** — поиск по ключевым словам с учетом опечаток (fuzzy matching)

5. **Группировка результатов** — результаты группируются по точности совпадения:
   - Точные совпадения (100%)
   - Хорошие совпадения (85%+)

### Архитектура модуля

Модуль разбит на отдельные классы согласно принципам ООП и модульности:

- **DocumentSelector** (112 строк) — выбор и группировка документов
- **DocumentDownloader** (228 строк) — загрузка документов с сервера
- **ArchiveExtractor** (287 строк) — извлечение архивов различных форматов
- **ExcelParser** (313 строк) — парсинг Excel файлов (.xlsx и .xls)
- **MatchFinder** (190 строк) — поиск совпадений товаров в документах
- **DocumentSearchService** (271 строка) — координатор, управляющий всеми модулями

### Использование

1. Откройте карточку торга
2. Нажмите кнопку "🔍 Поиск по документации"
3. Дождитесь завершения обработки (отображается прогресс)
4. Просмотрите результаты в диалоговом окне

### Текущий статус интеграции

- Карточки торгов (`modules/bids/tender_card.py`) подхватывают статус обработки напрямую из `tender_document_matches` и подсвечивают бейджи: зеленый для 100% совпадений, желтый для 85%, красный при отсутствии данных.
- Модуль корректно работает в связке с параллельным скриптом `scripts/run_document_processing.py`, который запускает пакетную загрузку, распаковку и парсинг документов.
- Консольный вывод документ-процессинга включает название файла, этап (скачивание, распаковка, парсинг, сохранение), итог по каждому файлу и тайминги, что облегчает отладку.
- Следующий этап (после доп. тестирования) — перенос логики в фоновый режим при запуске приложения, если пользователь заранее настроил источники данных. Реализация запланирована, но пока отключена.

## ♻️ Обновления мониторинга торгов (2025-12-16)

### Улучшение настроек поиска закупок

- Добавлена кнопка **"🔄 Обновить данные"** в настройках поиска закупок:
  - Применяет текущие настройки (регион, категория, ОКПД, стоп-слова) без сохранения в БД
  - Обновляет счетчики закупок на плитках дашборда в реальном времени
  - Позволяет тестировать настройки без перезапуска программы
  - Настройки не сохраняются в БД и будут сброшены после перезапуска

- Добавлена кнопка **"← Назад к дашборду"** в настройках поиска закупок:
  - Возвращает пользователя на дашборд закупок без сохранения настроек
  - Работает как в BidsWidget (через stack_widget), так и в PurchasesSubmenuWidget
  - Позволяет отменить изменения настроек без сохранения

- **Преимущества:**
  - Пользователь может экспериментировать с настройками и видеть результаты сразу
  - Не нужно перезапускать программу для применения настроек
  - Можно вернуться на дашборд без сохранения изменений
  - Кнопка "💾 Сохранить настройки" по-прежнему доступна для постоянного сохранения

## ♻️ Обновления мониторинга торгов (2025-12-16)

### Приоритизация обработки тендеров по размеру папок

- Добавлена оптимизация алгоритма обработки документов закупок: тендеры обрабатываются в порядке от меньших папок к большим.
- **Преимущества:**
  - Быстрее наполнение базы данных: сначала обрабатываются тендеры с малым объемом документов
  - Большие папки откладываются на потом, пока обрабатываются маленькие
  - В логах отображается размер папки для каждого тендера при обработке
- **Реализация:**
  - Добавлен метод `get_folder_size()` в `TenderFolderManager` для расчета размера папки
  - Метод `_prepare_tenders_with_sizes()` в `ArchiveBackgroundRunner` определяет размеры папок после скачивания
  - Тендеры сортируются по размеру папки перед обработкой (от меньшего к большему)

### Исправление логики очистки кэша закупок

- Исправлена проблема, когда кэш закупок очищался при каждом перезапуске приложения.
- **Изменения:**
  - Кэш очищается только если категория или регион действительно изменились (стали отличаться от закешированных)
  - При восстановлении категории/региона из кэша при загрузке приложения кэш не очищается
  - Добавлено логирование для отслеживания изменений категории/региона

## ♻️ Обновления мониторинга торгов (2025-11-27)

### Поддержка разыгранных торгов

- Добавлен параметр `tender_type` ('new' или 'won') во все компоненты обработки:
  - `TenderProvider.get_target_tenders()` — получает новые или разыгранные торги
  - `ArchiveBackgroundRunner.run()` — обрабатывает указанный тип торгов
  - `scripts/run_document_processing.py` — принимает `--tender-type` аргумент
  - UI автоматически определяет тип торгов по активной вкладке
- Исправлены синтаксические ошибки в модулях `workbook_manager.py` и `match_executor.py` (удалено дублирование кода)

## ♻️ Обновления мониторинга торгов (2025-11-26)

### Флаг интереса и сортировка

- В таблице `tender_document_matches` добавлен столбец `is_interesting` (BOOLEAN, по умолчанию NULL).
- Если по итогам анализа документ совпадений нет (`match_count = 0`), запись автоматически помечается как `FALSE`.
- Карточки торгов с `is_interesting = FALSE` больше не попадают в выдачу — интерфейс показывает только потенциально полезные закупки.
- Списки закупок сортируются по признаку обработки: сначала элементы с `processed_at`, затем по `start_date`.

### Новые вкладки «Разыгранные закупки»

- В `modules/bids/widget.py` появились отдельные вкладки для 44‑ФЗ и 223‑ФЗ.
- Отбор карточек:
  - `end_date < today` — торг уже завершён;
  - `delivery_end_date IS NOT NULL` и дата поставки ≥ 90 дней от сегодняшней;
  - учитываются те же фильтры ОКПД, регионов и стоп-слов пользователя.
- Разделы поддерживают всё существующее поведение: выбор карточек, запуск анализа, фильтрация категорий.

### Корректный выбор реестра и типа торгов при анализе

- Кнопка «Анализировать все» теперь передаёт `--registry-type` и `--tender-type` согласно активной вкладке:
  - **Новые закупки 44ФЗ/223ФЗ** → `--registry-type 44fz/223fz --tender-type new`
  - **Разыгранные закупки 44ФЗ/223ФЗ** → `--registry-type 44fz/223fz --tender-type won`
- Скрипт `scripts/run_document_processing.py` и `ArchiveBackgroundRunner` обрабатывают только соответствующий реестр и тип торгов, включая директории с ранее скачанными файлами.
- **Параллельная обработка:** Можно запускать несколько процессов параллельно без блокировок:
  - Каждый процесс работает в отдельном окне (`ProcessOutputDialog` с `setModal(False)`)
  - Каждый процесс создает свое подключение к БД
  - Каждый торг обрабатывается в своей папке (`{registry_type}_{tender_id}`)
  - Проверка "уже обработан" происходит на уровне БД перед началом обработки
  - Пример: новые 44‑ФЗ, новые 223‑ФЗ и разыгранные 44‑ФЗ будут работать независимо одновременно

### Быстрый старт анализа из консоли

```bash
# Все новые закупки пользователя только по 44‑ФЗ
python scripts/run_document_processing.py --user-id 1 --registry-type 44fz --tender-type new

# Все новые закупки по 223‑ФЗ
python scripts/run_document_processing.py --user-id 1 --registry-type 223fz --tender-type new

# Все разыгранные закупки по 44‑ФЗ
python scripts/run_document_processing.py --user-id 1 --registry-type 44fz --tender-type won

# Конкретные торги + анализ остальных того же реестра
python scripts/run_document_processing.py \
  --user-id 1 \
  --tenders "44fz:502187 223fz:134103" \
  --registry-type 223fz \
  --tender-type new \
  --all-after-priority
```

При наличии приоритетных ID скрипт сперва обработает их, затем запустит полный цикл по фильтрам пользователя.

**Параметры:**
- `--registry-type` — тип реестра (`44fz` или `223fz`)
- `--tender-type` — тип торгов (`new` для новых, `won` для разыгранных, по умолчанию `new`)
- `--tenders` — конкретные торги в формате `"44fz:123,456 223fz:789"`
- `--all-after-priority` — обработать все торги после приоритетных

## 🔄 Рефакторинг и улучшения кода (2024-11-18)

### Выполненные работы

#### 1. Рефакторинг модуля поиска по документации

**Проблема:** Монолитный файл `document_search_service.py` содержал 1548 строк кода, что затрудняло поддержку и отладку.

**Решение:** Разбит на 6 модулей согласно принципам ООП:
- Каждый класс не превышает 150 строк кода (исключая докстринги, аннотации, импорты, комментарии)
- Каждый модуль не превышает 150 строк кода (исключая докстринги, аннотации, импорты, комментарии)
- Четкое разделение ответственности (Single Responsibility Principle)

**Результат:**
- Было: 1 файл, 1548 строк
- Стало: 6 модулей, каждый до 313 строк
- Код стал модульным, проще поддерживать и тестировать

#### 2. Централизация настроек UI для PyQt5

**Проблема:** Дублирование настроек окон (setWindowTitle, resize, setMinimumSize) в разных файлах.

**Решение:** Создан модуль `modules/styles/ui_config.py` с централизованными функциями:
- `configure_dialog()` — настройка диалогов
- `configure_window()` — настройка главных окон
- Предустановленные размеры для разных типов диалогов

**Результат:**
- Все диалоги используют единые функции
- Нет дублирования кода настройки окон
- Легко изменить размеры всех диалогов в одном месте

#### 3. Устранение дублирования стилей (DRY принцип)

**Проблема:** Повторяющиеся вызовы `setStyleSheet()` с одинаковыми стилями в разных файлах.

**Решение:** Добавлены вспомогательные функции в `general_styles.py`:
- `apply_text_style_light()` — светлый текст
- `apply_text_style_dark()` — темный текст
- `apply_text_style_primary()` — основной цвет
- `apply_text_style_light_italic()` — светлый текст с курсивом
- `apply_font_weight()` — жирность шрифта

**Результат:**
- Заменено 18+ дублирующихся вызовов `setStyleSheet()`
- Код стал чище и проще в поддержке
- Изменение стилей теперь в одном месте

#### 4. Обновление правил проекта

Добавлены правила в `.cursorrules`:
- Максимальный размер класса: 150 строк кода (исключая докстринги, аннотации, импорты, комментарии)
- Максимальный размер модуля: 150 строк кода (исключая докстринги, аннотации, импорты, комментарии)
- Принцип DRY (Don't Repeat Yourself)
- Централизация настроек PyQt5 UI
- Примеры правильного и неправильного использования

### Статистика изменений

- **Рефакторено файлов:** 15+
- **Создано новых модулей:** 6
- **Устранено дублирований:** 25+
- **Добавлено функций:** 8
- **Улучшена читаемость:** все классы соответствуют правилам

## 📋 TODO: Планы развития системы

### 🔄 TODO: Модуль поиска по документации

### Приоритет 1: Анализ и сравнение цен

- [ ] **Отображение расчетов найденных материалов**
  - Показывать стоимость материалов из сметы
  - Показывать стоимость материалов из БД
  - Вычислять разницу (экономия/переплата)
  - Выдавать заключение о выгодности проекта

- [ ] **Интеграция с ценами из БД**
  - Получать актуальные цены товаров из базы данных
  - Учитывать различные варианты упаковки
  - Применять скидки и специальные предложения

### Приоритет 2: Автоматизация процесса

- [ ] **Автоматический поиск для новых торгов**
  - Настроить автоматический запуск поиска при добавлении новых торгов
  - Фоновый режим обработки
  - Уведомления о найденных совпадениях

- [ ] **Пересмотр при добавлении товаров**
  - Автоматический пересмотр предыдущих торгов при добавлении новых товаров в БД
  - Обновление результатов поиска
  - Уведомления о новых совпадениях

### Приоритет 3: Улучшение фильтрации

- [ ] **Альтернатива фильтрации по ОКПД**
  - Добавить фильтрацию по категориям проектов:
    - Проекты
    - Стройка
    - Мосты/путепроводы
    - Компьютеры
    - И другие категории
  - Подкатегории для более точной фильтрации
  - Множественный выбор категорий

### Приоритет 4: Управление интересными проектами

- [ ] **Отметка проектов в БД**
  - Возможность отмечать интересные/неинтересные проекты
  - Сохранение статуса в базе данных
  - Фильтрация по статусу интереса

- [ ] **Расчет потенциальной прибыли**
  - Отображение в карточке торга:
    - Количество найденных материалов
    - Стоимость материалов в смете
    - Стоимость материалов из БД
    - Потенциальная прибыль (разница)
    - Процент от общей стоимости проекта

- [ ] **Визуализация данных**
  - Графики соотношения стоимости
  - Индикаторы выгодности проекта
  - Сравнительные таблицы

### Приоритет 5: Улучшение UX

- [ ] **Расширенная информация в карточке торга**
  - Бейдж с количеством найденных материалов
  - Индикатор потенциальной прибыли
  - Быстрый просмотр результатов без открытия диалога

- [ ] **Экспорт результатов**
  - Экспорт найденных совпадений в Excel
  - Экспорт расчетов стоимости
  - Генерация отчетов

- [ ] **Настройки поиска**
  - Настройка порога совпадения
  - Выбор категорий товаров для поиска
  - Исключение определенных товаров

### 📧 TODO: Модуль работы с электронной почтой (общий)

#### Приоритет 1: Чтение и отправка писем

- [ ] **Интеграция с Яндекс почтой**
  - Реализация чтения входящих писем через IMAP или Яндекс Mail API
  - Настройка OAuth 2.0 для авторизации (если используется API)
  - Периодическая проверка новых писем (фоновая задача)
  - Отображение списка писем в UI

- [ ] **Отправка писем**
  - Реализация отправки через SMTP или Яндекс Mail API
  - Поддержка вложений (файлы, документы)
  - Сохранение истории отправленных писем

### Приоритет 2: ИИ-генерация ответов

- [ ] **Интеграция с OpenRouter**
  - Настройка API ключа OpenRouter
  - Выбор модели ИИ (GPT-4, Claude и др.)
  - Генерация ответов на основе входящих писем
  - Обработка контекста письма (тема, отправитель, история переписки)

- [ ] **UI для редактирования ответов**
  - Отображение исходного письма
  - Показ предложенного ответа от ИИ
  - Редактируемое поле для правок пользователем
  - Кнопка "Подправить" для повторной генерации с инструкциями
  - Кнопка "Отправить" для отправки письма

### Приоритет 3: Автоматизация и настройки

- [ ] **Настройки в .env**
  - `YANDEX_EMAIL` — адрес почты
  - `YANDEX_APP_PASSWORD` — пароль приложения для IMAP
  - `YANDEX_OAUTH_TOKEN` — токен OAuth (для API)
  - `OPENROUTER_API_KEY` — ключ API OpenRouter
  - `OPENROUTER_MODEL` — выбранная модель ИИ

- [ ] **Архитектура модуля**
  - `modules/email/email_reader.py` — чтение писем (IMAP/API)
  - `modules/email/email_sender.py` — отправка писем (SMTP/API)
  - `modules/email/email_ai_service.py` — интеграция с OpenRouter
  - `modules/email/email_widget.py` — UI компонент
  - `modules/email/email_config.py` — настройки и конфигурация

- [ ] **Обработка ошибок и безопасность**
  - Обработка сетевых ошибок
  - Обработка лимитов API
  - Безопасное хранение токенов в .env
  - Валидация входящих писем

### 💼 TODO: Расширение функциональности детальной карточки сделки (CRM)

#### Приоритет 0: Расширение раздела "КП / Товары" (редактирование и расчет)

- [ ] **Редактируемая таблица "Материалы из проектной документации"**
  - Пользователь вносит данные из проектной документации:
    - Наименование материала
    - Количество
    - Единица измерения (шт, кг, м³, м², и т.д.)
    - Цена за единицу (руб.)
  - Автоматический расчет "Итого" для каждой строки (Кол-во × Цена)
  - Справа отображается "Итого по материалам" (сумма всех строк)
  - Кнопка "💾 Сохранить материалы" для записи в БД
  - При открытии карточки сделки данные автоматически подгружаются из БД

- [ ] **Редактируемая таблица "Работы из проектной документации"**
  - Аналогично таблице "Материалы"
  - Пользователь вносит: Наименование работы, Объем, Ед. измерения, Цена за ед.
  - Автоматический расчет "Итого" для каждой строки
  - Справа отображается "Итого по работам"
  - Кнопка "💾 Сохранить работы" для записи в БД

- [ ] **Расширение таблицы `deal_item` в БД**
  - Добавить поле `item_type` VARCHAR(20) для различения типов позиций:
    - `'материал'` — материалы из проектной документации
    - `'работа'` — работы из проектной документации
    - `'товар_кп'` — товары для формирования КП из БД
  - Миграция SQL для добавления поля и обновления существующих записей

- [ ] **Модуль "Формирование КП из БД товаров"** (будущая функциональность)
  - Отдельная вкладка или раздел для формирования коммерческого предложения
  - Поиск товаров-аналогов по БД (таблица `products`):
    - Поиск по наименованию материала из проектной документации
    - Сопоставление с товарами из БД
    - Показ карточки товара с ценой, описанием, фото
  - Добавление выбранных товаров в "корзину КП"
  - Автоматический расчет итоговой стоимости КП:
    - Сумма всех товаров
    - Наценка/скидка (%)
    - Итоговая цена для клиента
  - Сохранение КП в таблицу `deal_item` с `item_type='товар_кп'`
  - Формирование PDF-документа с коммерческим предложением

#### Приоритет 1: Управление расходами по сделке

- [ ] **Вкладка "Отчет о расходах"**
  - Таблица расходов с категориями:
    - Командировки
    - Такси
    - Представительские расходы
    - Прочие расходы
  - Добавление новых расходов с указанием категории, суммы, даты, описания
  - Редактирование и удаление расходов
  - Суммирование расходов по категориям
  - Общая сумма расходов по сделке
  - Экспорт отчета в Excel

- [ ] **База данных для расходов**
  - Таблица `deal_expenses` в БД `tender_monitor`:
    - `id` — идентификатор расхода
    - `deal_id` — ID сделки (FK к `sales_deals`)
    - `category` — категория расхода (командировка, такси, представительские, прочие)
    - `amount` — сумма расхода
    - `expense_date` — дата расхода
    - `description` — описание расхода
    - `user_id` — пользователь, добавивший расход
    - `created_at` — дата создания записи
    - `updated_at` — дата обновления записи
  - Миграция SQL для создания таблицы
  - Индексы по `deal_id` и `category` для быстрого поиска

#### Приоритет 2: Задачник в сделке

- [ ] **Вкладка "Задачи"**
  - Список задач по сделке с фильтрацией:
    - Все задачи
    - Мои задачи (назначенные текущему пользователю)
    - Созданные мной
    - По статусу (новая, в работе, выполнена, отменена)
    - По приоритету (низкий, средний, высокий, критический)
  - Создание новой задачи:
    - Название задачи
    - Описание
    - Назначение исполнителю (выбор из списка пользователей БД по `user_id`)
    - Приоритет
    - Срок выполнения
    - Чеклист подзадач (опционально)
  - Редактирование и удаление задач
  - Изменение статуса задачи
  - Комментарии к задачам
  - История изменений задачи

- [ ] **База данных для задач**
  - Таблица `deal_tasks` в БД `tender_monitor`:
    - `id` — идентификатор задачи
    - `deal_id` — ID сделки (FK к `sales_deals`)
    - `title` — название задачи
    - `description` — описание задачи
    - `assignee_id` — ID исполнителя (FK к `users.id`)
    - `creator_id` — ID создателя задачи (FK к `users.id`)
    - `status` — статус (new, in_progress, completed, cancelled)
    - `priority` — приоритет (low, medium, high, critical)
    - `due_date` — срок выполнения
    - `created_at` — дата создания
    - `updated_at` — дата обновления
  - Таблица `deal_task_comments` для комментариев к задачам
  - Таблица `deal_task_history` для истории изменений

#### Приоритет 3: Командировки

- [ ] **Вкладка "Командировки"**
  - Список командировок по сделке
  - Создание новой командировки:
    - Название/цель командировки
    - Место назначения
    - Даты начала и окончания
    - Участники (выбор из списка пользователей)
    - Бюджет командировки
    - Описание
  - Редактирование и удаление командировок
  - Отчет по командировке:
    - Расходы на транспорт (такси, авиа, ж/д)
    - Расходы на проживание
    - Суточные
    - Представительские расходы
    - Прочие расходы
    - Итоговая сумма
  - Привязка расходов командировки к вкладке "Отчет о расходах"

- [ ] **База данных для командировок**
  - Таблица `deal_business_trips` в БД `tender_monitor`:
    - `id` — идентификатор командировки
    - `deal_id` — ID сделки (FK к `sales_deals`)
    - `title` — название/цель командировки
    - `destination` — место назначения
    - `start_date` — дата начала
    - `end_date` — дата окончания
    - `budget` — бюджет командировки
    - `description` — описание
    - `created_by` — ID создателя (FK к `users.id`)
    - `created_at` — дата создания
    - `updated_at` — дата обновления
  - Таблица `deal_business_trip_participants` для участников командировки
  - Связь с таблицей `deal_expenses` через поле `business_trip_id`

#### Приоритет 4: Почтовые сообщения в сделке

- [ ] **Вкладка "Почта"**
  - Список писем по сделке (цепочка переписки)
  - Отправка нового письма прямо из карточки сделки:
    - Кому (адресаты)
    - Тема письма (автоматически добавляется префикс с ID сделки)
    - Текст письма
    - Вложения (файлы)
    - Сохранение письма в БД с привязкой к `deal_id`
  - Получение писем по цепочке:
    - Автоматическое определение писем, относящихся к сделке (по теме, ID сделки в теме, адресатам)
    - Отображение всех писем цепочки в хронологическом порядке
    - Индикация прочитанных/непрочитанных писем
  - Интеграция с почтовым клиентом (Яндекс.Почта, Gmail и др.)
  - Фильтрация писем:
    - Входящие
    - Исходящие
    - Все письма
    - По дате
    - По отправителю/получателю

- [ ] **База данных для почты**
  - Таблица `deal_emails` в БД `tender_monitor`:
    - `id` — идентификатор письма
    - `deal_id` — ID сделки (FK к `sales_deals`)
    - `email_thread_id` — ID цепочки писем (для группировки)
    - `subject` — тема письма
    - `from_email` — адрес отправителя
    - `to_emails` — адреса получателей (JSONB массив)
    - `cc_emails` — адреса в копии (JSONB массив)
    - `body_text` — текст письма
    - `body_html` — HTML версия письма
    - `is_incoming` — входящее/исходящее письмо
    - `is_read` — прочитано ли письмо
    - `sent_at` — дата отправки/получения
    - `created_at` — дата создания записи
    - `metadata` — дополнительные данные (JSONB)
  - Таблица `deal_email_attachments` для вложений
  - Индексы по `deal_id`, `email_thread_id`, `sent_at`

#### Приоритет 5: Планирование и отчеты

- [ ] **Планирование звонков**
  - Вкладка "Звонки" в карточке сделки
  - Создание запланированного звонка:
    - Дата и время звонка
    - Участники (выбор из контактов сделки или пользователей)
    - Тема звонка
    - Описание/повод звонка
    - Напоминание (за X минут/часов до звонка)
  - Список запланированных звонков с фильтрацией по дате
  - Отметка о выполнении звонка
  - Отчет о звонке:
    - Длительность звонка
    - Результат звонка
    - Заметки по звонку
    - Следующие шаги

- [ ] **Планирование встреч**
  - Вкладка "Встречи" в карточке сделки
  - Создание запланированной встречи:
    - Дата и время встречи
    - Место встречи
    - Участники (контакты, пользователи)
    - Тема встречи
    - Описание
    - Напоминание
  - Список запланированных встреч
  - Отчет о встрече:
    - Результаты встречи
    - Принятые решения
    - Заметки
    - Следующие шаги

- [ ] **Отчеты по активности**
  - Вкладка "Отчеты" в карточке сделки
  - Сводный отчет по сделке:
    - Общая информация о сделке
    - Расходы по категориям (график)
    - Статистика задач (выполнено/в работе/новых)
    - Командировки (количество, общая сумма)
    - Звонки (количество, общая длительность)
    - Встречи (количество)
    - Почтовые сообщения (количество входящих/исходящих)
    - Активность по датам (график)
  - Экспорт отчета в PDF/Excel
  - Фильтрация отчета по периоду

- [ ] **База данных для планирования**
  - Таблица `deal_calls` в БД `tender_monitor`:
    - `id` — идентификатор звонка
    - `deal_id` — ID сделки (FK к `sales_deals`)
    - `scheduled_at` — запланированное время звонка
    - `completed_at` — время выполнения звонка
    - `duration_minutes` — длительность звонка
    - `subject` — тема звонка
    - `description` — описание
    - `result` — результат звонка
    - `notes` — заметки
    - `status` — статус (scheduled, completed, cancelled)
    - `created_by` — ID создателя
  - Таблица `deal_meetings` аналогично `deal_calls`
  - Таблицы для участников звонков и встреч

### 🎨 TODO: UI/UX улучшения (продолжение)

#### Фаза 1: Критические улучшения (MVP) — в процессе

- [x] **1.1. Улучшенные карточки сделок**
  - [x] Добавить цветовые индикаторы приоритета (реализовано как `border-left` с `COLORS['primary']`)
  - [x] Добавить название закупки, балансодержателя, регион поставки в карточку
  - [ ] Добавить аватар ответственного
  - [ ] Добавить индикатор дедлайна
  - [ ] Добавить теги
  - [ ] Добавить счетчики (файлы, комментарии)

- [x] **1.2. Детальная карточка сделки**
  - [x] Сделать более «CRM‑ный» summary‑блок в `DealDetailDialog`
  - [x] Разложить существующие данные по вкладкам более удобно
  - [x] Добавить регион закупки в summary-блок
  - [x] Добавить адрес поставки в summary-блок
  - [x] Убрать дублирующуюся информацию из блока "Сделка"
  - [x] Добавить вкладку "Документы закупки" с найденными фразами
  - [x] Добавить вкладку "Чат" с выбором собеседника и AI-агентом

- [ ] **1.3. Быстрые действия на карточках**
  - [ ] Hover меню с кнопками
  - [ ] Редактировать, копировать, удалить

- [ ] **1.4. Комментарии в карточке сделки**
  - [ ] Добавление комментариев
  - [ ] Просмотр истории комментариев
  - [ ] Упоминания пользователей

- [ ] **1.5. Прикрепление файлов**
  - [ ] Загрузка файлов к сделке
  - [ ] Просмотр списка файлов
  - [ ] Скачивание файлов

- [ ] **1.6. История изменений (вкладка "Активность")**
  - [ ] Отслеживание всех изменений
  - [ ] Временная шкала событий

#### Фаза 2: Расширенный функционал

- [ ] **2.1. Дашборд с аналитикой**
  - [ ] Визуализация воронок продаж
  - [ ] Статистика по сделкам
  - [ ] Графики и диаграммы

- [ ] **2.2. Глобальный поиск (Command Palette)**
  - [ ] Поиск по всем сделкам, контактам, задачам
  - [ ] Горячие клавиши для быстрого доступа

- [ ] **2.3. Центр уведомлений**
  - [ ] Уведомления о новых задачах
  - [ ] Уведомления о изменениях в сделках
  - [ ] Уведомления о новых письмах

- [ ] **2.4. Фильтры и группировка в канбане**
  - [ ] Фильтрация по статусу, приоритету, ответственному
  - [ ] Группировка по различным критериям

- [ ] **2.5. Теги и метки**
  - [ ] Добавление тегов к сделкам
  - [ ] Фильтрация по тегам
  - [ ] Цветовое кодирование тегов

#### Фаза 3: Автоматизация и интеграции

- [ ] **3.1. Чеклисты и подзадачи**
  - [ ] Создание чеклистов в задачах
  - [ ] Подзадачи с зависимостями

- [ ] **3.2. Календарь событий**
  - [ ] Календарный вид сделок
  - [ ] Планирование встреч и звонков

- [ ] **3.3. Автоматизация (триггеры)**
  - [ ] Автоматическое перемещение сделок по этапам
  - [ ] Автоматические уведомления
  - [ ] Автоматическое создание задач

- [ ] **3.4. Шаблоны сделок**
  - [ ] Создание шаблонов для типовых сделок
  - [ ] Быстрое создание сделок из шаблонов

- [ ] **3.5. Связи между сущностями**
  - [ ] Связывание сделок между собой
  - [ ] Связывание сделок с контактами и компаниями

## 📊 Модуль воронок продаж

### Описание

Модуль воронок продаж реализует управление сделками через канбан-доски в стиле Salesforce. Система поддерживает три типа воронок:

1. **Участие в торгах** — полный цикл от импорта до результата торгов
2. **Поставка материалов** — управление поставками материалов
3. **Субподрядные работы** — управление субподрядными проектами

### Архитектура модуля

Модуль построен по принципам модульности (каждый файл не более 200 строк):

- **models.py** (67 строк) — модели данных: `PipelineType`, `DealStatus`, `PipelineStage`, `Deal`
- **pipeline_repository.py** (145 строк) — репозиторий для работы с этапами воронок
- **deal_repository.py** (142 строки) — репозиторий для работы со сделками
- **submenu_widget.py** (95 строк) — подменю с тремя папками воронок
- **funnel_widget.py** (75 строк) — виджет для отображения конкретной воронки
- **kanban_board.py** (95 строк) — канбан-доска с колонками этапов
- **kanban_column.py** (95 строк) — колонка канбан-доски
- **deal_card.py** (85 строк) — карточка сделки

### Структура воронок

#### 1. Воронка участия в торгах

**Этапы:**
- **Этап 0**: Импорт и первичный фильтр (авто)
- **Этап 1**: Предварительный интерес
- **Этап 2**: Быстрый экономический скрининг
- **Этап 3**: Полный расчёт сметы
- **Этап 4**: Второе решение об участии
- **Этап 5**: Подготовка и подача заявки
- **Этап 6**: Торги/переторжка и результат

#### 2. Воронка по поставке материалов

**Этапы:**
- **Этап 1М**: Совпадение по материалам
- **Этап 2М**: Расчет коммерческого предложения по материалам
- **Этап 3М**: Переговоры и согласование
- **Этап 4М**: Заказ и отгрузки

#### 3. Воронка по субподрядным работам

**Этапы:**
- **Этап 1Р**: Попадание в периметр компетенций
- **Этап 2Р**: Черновой расчёт стоимости работ
- **Этап 3Р**: Детальная калькуляция и ресурсное планирование
- **Этап 4Р**: Коммерческое предложение по работам
- **Этап 5Р**: Договор субподряда и выполнение

### База данных

Модуль создает следующие таблицы в БД `tender_monitor`:

- **sales_pipeline_stages** — этапы воронок продаж
  - `id` — идентификатор этапа
  - `pipeline_type` — тип воронки (participation, materials_supply, subcontracting)
  - `stage_order` — порядок этапа (0, 1, 2, ...)
  - `name` — название этапа
  - `description` — описание этапа

- **sales_deals** — сделки в воронках
  - `id` — идентификатор сделки
  - `pipeline_type` — тип воронки
  - `stage_id` — текущий этап (FK к sales_pipeline_stages)
  - `tender_id` — связь с торгом (опционально)
  - `name` — название сделки
  - `description` — описание
  - `amount` — сумма сделки
  - `margin` — маржа (%)
  - `status` — статус (active, won, lost, archived)
  - `user_id` — владелец сделки
  - `metadata` — дополнительные данные (JSONB, в том числе `original_data` с данными закупки)

- **deal_item** — позиции КП/товаров внутри сделки
  - `deal_id` — ссылка на `sales_deals.id`
  - `product_name` — наименование из проекта
  - `product_code` — код номенклатуры/ОКПД (опционально)
  - `is_analog` — флаг "аналог" / "оригинал"
  - `unit` — единица измерения
  - `quantity` — количество
  - `price_per_unit` — цена за единицу
  - `total_price` — сумма строки (генерируемое поле)

- **contact** / **contact_link** — контакты и связи контактов:
  - `contact` хранит ФИО, отдел, должность, телефон, e-mail, заметки
  - `contact_link` связывает контакт с `customer`, `contractor` и/или `sales_deals` (гибкая M:N-связь)

- **customer_role** / **contractor_role** — роли контрагентов:
  - используются для аналитики и гибкой классификации заказчиков/подрядчиков (customer / contractor / supplier / designer)

- **deal_chat** — сообщения чата по сделкам:
  - `deal_id` — ID сделки (FK к `sales_deals`)
  - `sender_id` — ID пользователя-отправителя (NULL для AI-агента)
  - `sender_type` — тип отправителя: 'user' или 'ai_agent'
  - `message_text` — текст сообщения
  - `created_at` — время создания
  - `is_read` — прочитано ли сообщение
  - `metadata` — дополнительные данные (JSONB, например, тип AI-агента)

**Чат по сделке:**
- `modules/crm/sales_funnel/deal_chat_service.py` — сервис для работы с чатом (отправка/получение сообщений, загрузка списка пользователей)
- `modules/crm/sales_funnel/deal_chat_widget.py` — виджет чата с выбором собеседника (пользователь или AI-агент)
- `modules/crm/sales_funnel/deal_chat_ai_agent.py` — шаблонный AI-агент для общения в чате (в будущем можно интегрировать LLM API)

Миграция для этих таблиц находится в `scripts/sql_queries/add_crm_deal_extensions.sql` и выполняется в базе `tender_monitor`
однократно через скрипт `scripts/apply_crm_deal_extensions.py` (использует подключение к `tender_monitor` из `.env`).

Миграция для таблицы `deal_chat` находится в `scripts/sql_queries/add_deal_chat_table.sql` и выполняется через скрипт `scripts/apply_deal_chat_migration.py`.

После применения миграции таблицы не создаются динамически кодом, а считаются частью постоянной схемы CRM.

### Детальная карточка сделки (CRM)

Для работы с расширенными данными сделки реализованы:

- `modules/crm/sales_funnel/deal_detail_service.py` — агрегирует данные из:
  - `sales_deals` (основная информация о сделке),
  - реестра закупок (через `metadata.original_data`),
  - `customer` / `contractor`,
  - `contact` + `contact_link`,
  - `deal_item`.
- `modules/crm/sales_funnel/deal_detail_dialog.py` — диалоговая форма с вкладками:
  - **Общая информация** (тендер + сделка, summary-блок с ключевой информацией: суммы, маржа, статус, регион, адрес поставки, реестр, номер закупки, ответственный),
  - **Заказчик** (данные компании + связанные контакты),
  - **Подрядчик** (данные компании + связанные контакты),
  - **КП / Товары** (позиции `deal_item`),
  - **💬 Чат** (чат по сделке с выбором собеседника и AI-агентом),
  - **📄 Документы закупки** (найденные фразы из документации закупки).

**Улучшения карточки сделки в воронке (DealCard):**
- Отображение названия закупки (`auction_name`)
- Отображение балансодержателя (заказчика)
- Отображение региона поставки (`delivery_region` или `region_name`)

**Улучшения детальной карточки сделки:**
- Summary-блок с ключевой информацией в двух колонках
- Адрес поставки (`delivery_region` и `delivery_address`) в summary-блоке
- Регион закупки в summary-блоке
- Вкладка "Документы закупки" с найденными фразами из документации
- Вкладка "Чат" с выбором собеседника и AI-агентом
- Убрана дублирующаяся информация из блока "Сделка"

Открытие детальной карточки:

- двойной клик по карточке сделки (`DealCard`) в канбан‑воронке,
- событие обрабатывается в `SalesFunnelKanbanBoard.on_deal_clicked`,
- данные подготавливаются через `DealDetailService` и отображаются в `DealDetailDialog`.

### Использование

1. Откройте раздел **CRM** в боковом меню
2. Кликните на папку **"Воронка продаж"** (двойной клик)
3. Выберите нужную воронку:
   - **Участвовать** — воронка участия в торгах
   - **Поставка материалов** — воронка поставки материалов
   - **Суб-подрядные работы** — воронка субподрядных работ
4. В канбан-доске отображаются все сделки, сгруппированные по этапам
5. Используйте кнопку **"➕ Добавить сделку"** для создания новой сделки

### Тестирование

Модуль включает unit-тесты:

- `tests/test_sales_funnel_models.py` — тесты моделей данных
- `tests/test_sales_funnel_repositories.py` — тесты репозиториев

Запуск тестов:
```bash
pytest tests/test_sales_funnel_models.py
pytest tests/test_sales_funnel_repositories.py
```

## 📝 Лицензия

Проект разработан для внутреннего использования.

## 👥 Авторы

Разработано командой B2B AutoDesk.
