# Отчет о дублирующих методах в модулях

## 🔍 Найденные дублирования

### 1. ДУБЛИРОВАНИЕ: Функции получения данных совпадений

**Файлы:**
- `modules/bids/tender_card_data.py` - функции `fetch_match_summary()` и `fetch_match_details()`
- `modules/bids/tender_card_data_fetch.py` - функции `fetch_match_summary_with_cache()` и `fetch_match_details_with_cache()`

**Проблема:**
- Два модуля с похожей функциональностью
- `tender_card_data.py` - простая версия с кэшем
- `tender_card_data_fetch.py` - версия с кэшем (более полная)

**Использование:**
- `tender_card_data_fetch.py` используется в `tender_card.py` (основной код)
- `tender_card_data.py` - возможно, устаревший код

**Рекомендация:**
- ✅ Удалить `modules/bids/tender_card_data.py` (устаревший)
- ✅ Использовать только `tender_card_data_fetch.py`

---

### 2. ДУБЛИРОВАНИЕ: Функции настройки размеров UI

**Файл:**
- `modules/styles/ui_config.py`

**Проблема:**
- Методы класса `WindowConfig` дублируются функциями-обертками:
  - `WindowConfig.set_dialog_size()` ↔ `set_dialog_size()`
  - `WindowConfig.set_window_size()` ↔ `set_window_size()`
  - `WindowConfig.set_minimum_size()` ↔ `set_minimum_size()`
  - `WindowConfig.set_maximum_size()` ↔ `set_maximum_size()`
  - `WindowConfig.configure_dialog()` ↔ `configure_dialog()`
  - `WindowConfig.configure_window()` ↔ `configure_window()`

**Использование:**
- Функции-обертки используются для удобства (не нужно создавать экземпляр класса)
- Методы класса используются редко

**Рекомендация:**
- ✅ Оставить функции-обертки (более удобны)
- ⚠️ Методы класса можно оставить для обратной совместимости, но пометить как deprecated

---

### 3. ДУБЛИРОВАНИЕ: Поиск дополнительных фраз в разных форматах

**Файлы:**
- `services/document_search/excel_additional_phrases.py` - `search_additional_phrases_in_excel()`
- `services/document_search/pdf_search.py` - `search_additional_phrases_in_pdf()`
- `services/document_search/match_finder.py` - `search_additional_phrases_in_word()`

**Проблема:**
- Похожая логика поиска фраз в разных форматах документов
- Каждая функция делает одно и то же: ищет фразы в тексте

**Использование:**
- Используются в `MatchFinder` для разных типов документов

**Рекомендация:**
- ⚠️ Вынести общую логику поиска фраз в отдельную функцию
- ✅ Оставить обертки для каждого формата (для удобства использования)

**Предложение:**
```python
# services/document_search/additional_phrases_search.py
def search_phrases_in_text(text: str, phrases: List[str]) -> List[Dict[str, Any]]:
    """Общая функция поиска фраз в тексте"""
    # Общая логика

# Обертки для каждого формата
def search_additional_phrases_in_excel(file_path: Path) -> List[Dict[str, Any]]:
    text = extract_text_from_excel(file_path)
    return search_phrases_in_text(text, get_additional_search_phrases())
```

---

### 4. ДУБЛИРОВАНИЕ: Извлечение текста из документов

**Файлы:**
- `services/document_search/document_parser.py` - `extract_text()` для Word
- `services/document_search/pdf_processor.py` - `extract_text()` для PDF
- `services/document_search/word_processor.py` - `extract_text()` для Word

**Проблема:**
- Методы с одинаковым именем в разных классах
- Разная реализация для разных форматов

**Использование:**
- Используются в разных контекстах

**Рекомендация:**
- ✅ Это НЕ дублирование - разные классы для разных форматов
- ✅ Оставить как есть (правильная архитектура)

---

### 5. ДУБЛИРОВАНИЕ: Подготовка путей к Excel файлам

**Файлы:**
- `services/document_search/workbook_preparator.py` - `prepare_workbook_paths()`
- `services/document_search/search_coordinator.py` - `prepare_workbook_paths()`

**Проблема:**
- Два класса с одинаковым методом
- Похожая логика подготовки путей

**Использование:**
- `workbook_preparator.py` - отдельный класс
- `search_coordinator.py` - часть координатора поиска

**Рекомендация:**
- ⚠️ Проверить, используется ли `workbook_preparator.py`
- ✅ Если не используется - удалить
- ✅ Если используется - вынести общую логику в отдельную функцию

---

### 6. ДУБЛИРОВАНИЕ: Скачивание документов

**Файлы:**
- `services/document_search/document_downloader.py` - `download_document()`, `download_required_documents()`
- `services/document_search/document_downloader_coordinator.py` - `download_documents()`
- `services/document_search/search_coordinator.py` - `download_documents()`

**Проблема:**
- Несколько классов для скачивания документов
- Похожая функциональность

**Использование:**
- `document_downloader.py` - базовый класс
- `document_downloader_coordinator.py` - координатор скачивания
- `search_coordinator.py` - координатор поиска (использует downloader)

**Рекомендация:**
- ✅ Это НЕ дублирование - разные уровни абстракции
- ✅ `document_downloader.py` - базовый класс
- ✅ Координаторы используют базовый класс

---

### 7. ДУБЛИРОВАНИЕ: Извлечение данных строки Excel

**Файлы:**
- `services/document_search/xls_row_extractor.py` - `extract_xls_row_data()`
- `services/document_search/xlsx_row_extractor.py` - `extract_xlsx_row_data()`
- `services/document_search/xlsx_row_data_processor.py` - `extract_row_data_from_values_xlsx()`
- `services/document_search/full_row_extractor.py` - `extract_full_row_xls()`, `extract_full_row_xlsx()`
- `services/document_search/excel_parser.py` - `extract_row_data()`, `extract_full_row_with_context()`

**Проблема:**
- Много модулей для извлечения данных из Excel
- Похожая функциональность в разных местах

**Использование:**
- Разные модули для разных форматов (XLS vs XLSX)
- Разные уровни детализации (простая строка vs полная строка с контекстом)

**Рекомендация:**
- ⚠️ Проверить использование каждого модуля
- ✅ Если модули не используются - удалить
- ✅ Если используются - оставить (разные форматы и уровни детализации)

---

## 📊 Сводная таблица дублирований

| № | Тип дублирования | Файлы | Критичность | Рекомендация |
|---|------------------|-------|-------------|--------------|
| 1 | Функции получения данных | `tender_card_data.py` vs `tender_card_data_fetch.py` | Высокая | Удалить устаревший файл |
| 2 | Функции настройки UI | `ui_config.py` (методы vs функции) | Низкая | Оставить функции, пометить методы как deprecated |
| 3 | Поиск дополнительных фраз | `excel_additional_phrases.py`, `pdf_search.py`, `match_finder.py` | Средняя | Вынести общую логику |
| 4 | Извлечение текста | `document_parser.py`, `pdf_processor.py`, `word_processor.py` | Нет | Разные классы - нормально |
| 5 | Подготовка путей Excel | `workbook_preparator.py` vs `search_coordinator.py` | Средняя | Проверить использование |
| 6 | Скачивание документов | Несколько координаторов | Нет | Разные уровни абстракции - нормально |
| 7 | Извлечение данных Excel | Много модулей | Средняя | Проверить использование каждого |

---

## ✅ Приоритетные действия

### Высокий приоритет:
1. **Удалить `modules/bids/tender_card_data.py`** - устаревший код
   - Проверить, что нигде не используется
   - Удалить файл

### Средний приоритет:
2. **Рефакторинг поиска дополнительных фраз:**
   - Вынести общую логику в `additional_phrases_search.py`
   - Обновить обертки для каждого формата

3. **Проверить использование модулей извлечения данных Excel:**
   - Проверить, какие модули реально используются
   - Удалить неиспользуемые

### Низкий приоритет:
4. **Пометить методы класса `WindowConfig` как deprecated:**
   - Добавить `@deprecated` декоратор
   - Оставить для обратной совместимости

---

## 🔍 Дополнительные проверки

### Рекомендуется проверить:
1. Использование `workbook_preparator.py` в коде
2. Использование всех модулей извлечения данных Excel
3. Есть ли другие места, где используется `tender_card_data.py`

### Команды для проверки:
```bash
# Поиск использования tender_card_data.py
grep -r "tender_card_data" --exclude-dir=venv --exclude-dir=__pycache__

# Поиск использования workbook_preparator
grep -r "workbook_preparator" --exclude-dir=venv --exclude-dir=__pycache__

# Поиск использования всех модулей извлечения Excel
grep -r "xls_row_extractor\|xlsx_row_extractor\|full_row_extractor" --exclude-dir=venv --exclude-dir=__pycache__
```

