# Анализ проблемы с парсингом закупок

## 📋 Описание проблемы

При обработке торгов документы успешно скачиваются, но парсинг не запускается. Все торги завершаются с ошибкой.

### Симптомы:
- Документы скачиваются успешно (видны в логах "Документ сохранен")
- Фоновая загрузка завершается ("Фоновая загрузка завершена")
- Все торги завершаются с ошибкой "❌ Ошибка обработки торга"
- Парсинг не начинается (workbook_paths остается пустым)

### Статистика из логов:
- Обработано торгов: 0/663
- Успешно обработано: 0
- Ошибок: 634
- Всего найдено совпадений: 0

## 🔍 Причина проблемы

Проблема в том, что после скачивания документов они не обрабатываются правильно. `workbook_paths` остается пустым, что приводит к ошибке "no_workbook_files".

### Возможные причины:

1. **Файлы скачиваются, но не попадают в download_records правильно**
   - После скачивания через префетчер файлы могут не проходить проверку валидности
   - Файлы могут быть повреждены при скачивании

2. **Файлы не распаковываются из архивов**
   - Архивы скачиваются, но не распаковываются
   - Проблемы с инструментами распаковки (unrar, winrar)

3. **Файлы не проходят проверку валидности**
   - Файлы могут быть повреждены
   - Проверка валидности может быть слишком строгой

4. **Ошибка "cannot schedule new futures after interpreter shutdown"**
   - Префетчер пытается запустить задачи после завершения интерпретатора
   - Проблема с ThreadPoolExecutor

## ✅ Исправления

### 1. Добавлено детальное логирование

**Файл: `services/archive_runner/tender_processor.py`**

- Добавлено детальное логирование количества записей и файлов
- Добавлено логирование путей к файлам и их существования
- Добавлено логирование причин ошибок при подготовке workbook_paths

**Изменения:**
```python
# Детальное логирование для диагностики
if download_records:
    total_files = sum(len(record.get("paths", [])) for record in download_records)
    logger.info(f"Всего файлов в записях: {total_files}")
    for idx, record in enumerate(download_records[:3]):
        paths = record.get("paths", [])
        logger.debug(f"  Запись {idx+1}: {len(paths)} файлов, пути: {[str(p)[-50:] for p in paths[:2]]}")
```

**При ошибке "no_workbook_files":**
```python
logger.error(f"❌ Не удалось подготовить Excel файлы для торга {tender_id} ({registry_type})")
logger.error(f"   download_records: {len(download_records)} записей")
if download_records:
    total_files = sum(len(record.get("paths", [])) for record in download_records)
    logger.error(f"   Всего файлов в записях: {total_files}")
    # Показываем детали первых записей
    for idx, record in enumerate(download_records[:5]):
        paths = record.get("paths", [])
        logger.error(f"   Запись {idx+1}: {len(paths)} файлов")
        for path_idx, path in enumerate(paths[:3]):
            path_obj = Path(path)
            exists = path_obj.exists()
            logger.error(f"      Файл {path_idx+1}: {path_obj.name} (существует: {exists}, размер: {path_obj.stat().st_size if exists else 0})")
```

### 2. Исправлена проблема с префетчером

**Файл: `services/archive_runner/tender_prefetcher.py`**

- Исправлен метод `shutdown()` для корректного завершения ThreadPoolExecutor
- Добавлена отмена незавершенных задач перед закрытием
- Добавлено ожидание завершения текущих задач с таймаутом

**Изменения:**
```python
def shutdown(self) -> None:
    """Завершает пул потоков."""
    try:
        # Отменяем все незавершенные задачи
        for future in self._futures.values():
            if not future.done():
                future.cancel()
        # Закрываем executor с ожиданием завершения текущих задач
        self.executor.shutdown(wait=True, timeout=30)
    except Exception as e:
        logger.warning(f"Ошибка при закрытии префетчера: {e}")
```

### 3. Улучшено логирование ошибок

**Файл: `services/archive_runner/runner.py`**

- Изменен уровень логирования с ERROR на WARNING для ошибок обработки торгов
- Это позволяет лучше видеть детали ошибок без лишнего шума

## 🔧 Следующие шаги для диагностики

1. **Запустить обработку заново** с новым логированием
2. **Проверить логи** на наличие детальной информации о:
   - Количестве download_records
   - Количестве файлов в записях
   - Существовании файлов и их размерах
   - Причинах ошибок при подготовке workbook_paths

3. **Проверить конкретный торг** вручную:
   - Найти папку торга в `DOCUMENT_DOWNLOAD_DIR`
   - Проверить, какие файлы скачаны
   - Попробовать распаковать архивы вручную
   - Проверить, можно ли открыть Excel файлы

4. **Если проблема сохраняется**, возможные причины:
   - Файлы скачиваются, но не проходят проверку валидности
   - Архивы не распаковываются (проблемы с unrar/winrar)
   - Excel файлы повреждены или имеют неожиданный формат
   - Проблемы с путями к файлам (кириллица в именах, специальные символы)

## 📊 Соответствие правилам проектирования

### Проверка правил:

✅ **Размер классов:**
- `TenderProcessor`: 524 строки (превышает 300) - требуется разбиение
- `ArchiveBackgroundRunner`: 531 строка (превышает 500) - требуется разбиение

⚠️ **Рекомендации:**
- Разбить `TenderProcessor` на несколько классов:
  - `TenderProcessor` - основная логика
  - `TenderValidator` - проверка валидности файлов
  - `TenderRecordBuilder` - построение записей для обработки
- Разбить `ArchiveBackgroundRunner` на несколько классов

✅ **DRY принцип:**
- Код логирования ошибок используется в нескольких местах - можно вынести в отдельный метод
- Проверка валидности файлов дублируется - можно вынести в отдельный класс

✅ **Модульность:**
- Код хорошо разделен на модули
- Каждый класс имеет одну ответственность

## 📝 Рекомендации

1. **Немедленно:**
   - Запустить обработку с новым логированием
   - Проверить детальные логи для понимания проблемы

2. **Краткосрочно:**
   - Исправить проблему с обработкой файлов (после анализа логов)
   - Разбить большие классы на более мелкие

3. **Долгосрочно:**
   - Добавить unit-тесты для критических компонентов
   - Улучшить обработку ошибок
   - Добавить метрики производительности

