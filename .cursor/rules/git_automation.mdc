***
alwaysApply: true
***

# Правила автоматизации Git для проекта B2B AutoDesk

## 🚀 Автоматический Git Push

После каждого изменения в проекте AI должен автоматически выполнять git push с осмысленными комментариями.

## 📋 Правила выполнения Git операций

### 1. Автоматический Commit и Push

**Когда выполнять:**
- После завершения любой задачи (изменения кода, добавление файлов, обновление документации)
- После исправления ошибок
- После обновления зависимостей
- После обновления README или документации

**ЧТО НЕ ДЕЛАТЬ:**
- ❌ НЕ делать commit/push для временных файлов (*.pyc, __pycache__, *.log)
- ❌ НЕ делать commit/push для .env файла (содержит секреты)
- ❌ НЕ делать commit/push для venv/ директории
- ❌ НЕ делать commit/push для IDE конфигураций (.idea/, .vscode/)

### 2. Формат Commit сообщений

**Структура сообщения:**
```
[Тип] Краткое описание изменений

Детальное описание изменений (если необходимо):
- Что было изменено
- Почему было изменено
- Какие файлы затронуты
```

**Типы изменений:**
- `[FEATURE]` - Добавление новой функциональности
- `[FIX]` - Исправление ошибок/багов
- `[REFACTOR]` - Рефакторинг кода (без изменения функциональности)
- `[CONFIG]` - Изменение конфигурации
- `[DOCS]` - Обновление документации
- `[DEPS]` - Обновление зависимостей
- `[STYLE]` - Изменения форматирования кода
- `[TEST]` - Добавление/изменение тестов

**Примеры хороших commit сообщений:**

```
[FEATURE] Добавлена поддержка распаковки ZIP, RAR и 7Z архивов

- Добавлена библиотека py7zr для работы с 7Z архивами
- Обновлен DocumentSearchService для поддержки всех форматов
- Добавлена правильная обработка кодировок для ZIP (cp437 → cp866)
- Обновлены тесты для новой функциональности

Файлы:
- services/document_search_service.py
- requirements.txt
- tests/test_document_search_service.py
```

```
[CONFIG] Перенесены все локальные пути в .env файл

- Добавлены переменные окружения DOCUMENT_DOWNLOAD_DIR и WINRAR_PATH
- Обновлен config/settings.py для загрузки путей из .env
- Удалены хардкод пути из modules/bids/widget.py
- Обновлен README.md с описанием новых переменных окружения

Файлы:
- config/settings.py
- modules/bids/widget.py
- services/document_search_service.py
- README.md
```

```
[FIX] Исправлена ошибка обработки многофайловых RAR архивов

- Добавлена обработка исключения NeedFirstVolume
- Улучшено логирование ошибок распаковки
- Обновлена документация по работе с архивами

Файлы:
- services/document_search_service.py
```

### 3. Процесс выполнения Git операций

**Алгоритм:**

1. **Проверка статуса:**
   ```bash
   git status
   ```

2. **Добавление файлов:**
   ```bash
   git add <измененные_файлы>
   ```
   Или для всех изменений (кроме игнорируемых):
   ```bash
   git add .
   ```

3. **Создание commit:**
   ```bash
   git commit -m "[ТИП] Описание" -m "Детальное описание"
   ```

4. **Push в репозиторий:**
   ```bash
   git push
   ```

### 4. Проверка перед Push

**Перед выполнением push проверь:**

1. ✅ Все изменения соответствуют запросу пользователя
2. ✅ Нет временных/служебных файлов в коммите
3. ✅ Commit сообщение понятное и информативное
4. ✅ Код не содержит хардкод секретов/паролей
5. ✅ Все пути настраиваются через .env

### 5. Обработка ошибок Git

**Если push не удался:**

1. Проверь наличие удаленных изменений:
   ```bash
   git pull
   ```

2. Разреши конфликты (если есть)

3. Повтори push:
   ```bash
   git push
   ```

**Если есть незакоммиченные изменения:**
- Сначала закоммить текущие изменения
- Затем выполнить pull и разрешить конфликты
- После этого выполнить push

### 6. Игнорируемые файлы (.gitignore)

**Убедись, что .gitignore содержит:**
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db
```

### 7. Специальные случаи

**Большие изменения (множество файлов):**
- Разбей на несколько логических коммитов
- Каждый коммит должен иметь осмысленное описание

**Обновление зависимостей:**
- Отдельный коммит с типом `[DEPS]`
- Укажи какие зависимости обновлены и почему

**Рефакторинг:**
- Отдельный коммит с типом `[REFACTOR]`
- Укажи что именно было рефакторено

## 📝 Шаблон для AI

**После каждого изменения кода выполни:**

```bash
# 1. Проверка статуса
git status

# 2. Добавление изменений
git add <список_измененных_файлов>

# 3. Создание коммита с описательным сообщением
git commit -m "[ТИП] Краткое описание" -m "Детальное описание изменений:
- Что изменено
- Почему изменено
- Какие файлы затронуты"

# 4. Push в репозиторий
git push
```

## ⚠️ Важные замечания

1. **НЕ коммить .env файл** - он содержит секреты
2. **НЕ коммить venv/** - виртуальное окружение
3. **НЕ коммить временные файлы** - логи, кэш, временные файлы
4. **Всегда проверяй** что коммитишь перед push
5. **Используй осмысленные сообщения** - они помогают понять историю изменений

## 🎯 Цель

Автоматизация Git операций должна:
- Сохранять историю изменений проекта
- Делать код доступным для других разработчиков
- Упрощать отслеживание изменений
- Обеспечивать резервное копирование кода
